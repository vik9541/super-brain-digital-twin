# 🧠 SUPER BRAIN — ТЕХНИЧЕСКОЕ ЗАДАНИЕ v2.0

**Дата:** 7 декабря 2025, 13:45 MSK  
**Статус:** ✅ УТВЕРЖДЕНО  
**Версия:** 2.0  
**Платформа:** Kubernetes (DigitalOcean DOKS) + Telegram  
**Концепция:** Персональный AI Assistant через Telegram

---

## 📌 ГЛАВНАЯ ИДЕЯ

**ALL-IN-ONE INTERFACE = BOT.PY**

Всё общение с цифровым помощником идёт через Telegram:

```
Ты загружаешь знания (файлы, текст, голос)
                ↓
         BOT.PY анализирует
                ↓
     Если не понимает → спрашивает
         ты уточняешь
                ↓
    Всё сохраняется в БД
                ↓
      [Ночью 2:00 AM]
     BATCH_ANALYZER эволюционирует
      и генерирует инсайты
                ↓
      Ты просишь задачи → Бот выдаёт
      Ты спрашиваешь → Бот отвечает
                ↓
         ✅ ГОТОВО!
```

---

## 🤖 BOT.PY — Основной Сервис (600-900 строк)

### Функция #1: Загрузка Любого Контента

```
Пользователь: [загружает file.pdf]
Бот: "📄 Получил файл (2.5 MB). Что это?"

Пользователь: "Смета на проект MOS-01"
Бот: ✅ Сохранено!
     📌 Проект: MOS-01
     📂 Категория: смета
     🏷️ Теги: финансы, MOS-01, сметы
     🔗 Путь: s3://bucket/2025/12/smeta_mos01.pdf
     💬 Комментарий сохранён
```

**Поддерживает:**
- 📄 PDF, DOC, DOCX, XLS, XLSX, PPT
- 🖼️ Изображения (JPG, PNG, GIF, SVG)
- 🎵 Аудио (MP3, WAV, OGG) + автотранскрипция
- 📝 Текст (неограниченный размер)
- 📊 CSV, JSON, XML
- 🎥 Видео (сохраняются метаданные)

---

### Функция #2: Интеллектуальная Классификация + Уточнения

**Сценарий 1: Простой файл**
```
Пользователь: [загружает document.pdf]
Бот: "🤖 Это финансовый отчет? (confidence: 92%)"
Пользователь: ✓ Да
Бот: ✅ Принято! Сохранено как "финансовый отчет"
```

**Сценарий 2: Сложный файл**
```
Пользователь: [загружает странный файл]
Бот: "🤔 Не очень понимаю что это (confidence: 35%)"
Бот: "Выбери из вариантов:"
     1️⃣ Финансовый документ
     2️⃣ Техническая спецификация
     3️⃣ Письмо / Email
     4️⃣ Что-то другое

Пользователь: "Это техническая спецификация"
Бот: ✅ Обновил классификацию!
     Категория: техническая спецификация
     Confidence: 95%
```

**Сценарий 3: Очень сложный**
```
Пользователь: [загружает непонятный файл]
Бот: "🤔 Даже не знаю... Объясни?"

Пользователь: 🎤 [говорит голосом 30 секунд]
Бот: 📝 Транскрипция: "Это план работ на следующий квартал"
     ✅ Классифицировано как "план работ"
```

**Процесс пока не будет ясно:**
```
1. Загрузка → Perplexity классифицирует (confidence %)
2. Если confidence < 70% → спрашивает у пользователя
3. Пользователь отвечает (выбор / текст / голос)
4. Обновляет классификацию
5. Если всё ещё < 85% → спрашивает ещё раз
6. Loop пока не будет >= 95% ИЛИ пользователь скажет "стоп"
```

---

### Функция #3: Управление Задачами на День

```
Команда: /today
Бот: 📋 ЗАДАЧИ НА СЕГОДНЯ (7 дек 2025)
     
     🔴 СРОЧНО:
     1. Встреча с Иваном в 14:00 (МOS-01)
     2. Подготовить смету до 18:00
     
     🟡 ВАЖНО:
     3. Ответить Марии на письмо
     4. Обновить статус MOS-01
     
     🟢 МОЖНО ПОЗЖЕ:
     5. Загрузить отчет в облако
     6. Обновить контакты
     
     ✅ ЗАВЕРШЁННЫЕ (3):
     - Звонок Грегори
     - Email команде
     - Проверка документов

Команда: /add_task Позвонить Игорю в 17:00
Бот: ✅ Задача добавлена
     Приоритет: средний (или /priority высокий)
     Проект: не указан (или /object MOS-01)

Команда: /done 2
Бот: ✅ Смета отмечена как готова!
     Завершено: 7 дек 2025, 15:30
```

**Где хранятся:**
- Таблица `tasks` в Supabase
- Поля: title, status, priority, due_date, object_id, completed_at

---

### Функция #4: Ответы на Вопросы из БД

```
Пользователь: "Сколько встреч с Иваном?"
Бот: 👥 АНАЛИЗ:
     - Встреч всего: 12
     - Период: последние 3 месяца
     - По темам:
       • Финансы/бюджет: 8 встреч
       • Планирование: 4 встречи
     - Последняя встреча: 3 дек 2025
     - Следующая встреча: не запланирована

Пользователь: "Кто работает над MOS-01?"
Бот: 👥 КОМАНДА ПРОЕКТА:
     - Иван Петров (8 упоминаний) - финансы
     - Мария Сидорова (6) - планирование
     - Дмитрий Волков (3) - логистика
     - Сергей Смирнов (2) - согласование

Пользователь: "Какие финансовые файлы я загружал?"
Бот: 📄 ФИНАНСОВЫЕ ДОКУМЕНТЫ:
     1. smeta_mos01.pdf (15.5 MB) - 5 дек
     2. budget_q4.xlsx (3.2 MB) - 1 дек
     3. договор_renovaciia.docx (2.1 MB) - 28 ноя

Пользователь: "Какие встречи планируются на неделю?"
Бот: 📅 ВСТРЕЧИ НА НЕДЕЛЮ:
     пн 8 дек: Иван (14:00) - MOS-01
     ср 10 дек: Маша (11:00) - Бюджет
     чт 11 дек: Дмитрий (15:30) - Логистика
```

**Алгоритм:**
```
1. Парсит вопрос
2. Определяет сущность (человек, проект, дату, категорию)
3. Ищет в БД (events, contacts, files, tasks)
4. Агрегирует результаты
5. Форматирует красиво и выдаёт
```

---

### Функция #5: Управление Объектами (Проектами)

```
Команда: /object:MOS-01
Бот: ✅ Активный объект установлен: MOS-01
     📊 Статистика:
     - Создан: 1 сентября 2025
     - Участников: 4 человека
     - Событий: 12
     - Файлов: 8
     - Статус: 80% готово

Пользователь: "Добавить Сергея"
Бот: ✅ Сергей добавлен к проекту MOS-01
     
Пользователь: "Изменить статус на 90%"
Бот: ✅ Статус MOS-01 обновлён: 90%
     
Пользователь: "Бюджет проекта $50000"
Бот: ✅ Бюджет установлен: $50,000
```

---

### Функция #6: Сохранение Всех Файлов и Комментариев

**Принцип:** НИЧЕГО не удаляется, всё версионируется

```
Таблица files_metadata:
{
    "id": 1001,
    "file_name": "smeta_mos01.pdf",
    "file_hash": "sha256:abc123def456...",
    "file_size_bytes": 15500000,
    "file_type": "pdf",
    "storage_path": "s3://bucket/files/2025/12/07/smeta_mos01.pdf",
    
    "user_comment": "Это смета на проект MOS-01",
    "ai_classification": {
        "category": "смета",
        "type": "финансовый документ",
        "confidence": 97,
        "keywords": ["смета", "финансы", "MOS-01", "бюджет"]
    },
    
    "related_object": "MOS-01",
    "related_people": ["Ivan Petrov", "Maria Sidorova"],
    "created_at": "2025-12-07 13:45:00",
    "uploaded_by": "user_123",
    
    "versions": [
        {"version": 1, "hash": "...", "uploaded": "2025-12-07 13:45"},
        {"version": 2, "hash": "...", "uploaded": "2025-12-07 14:10"}
    ]
}
```

---

### BOT.PY — Главные Классы

```python
class SuperBrainBot:
    # Обработка входящих данных
    async def on_message(self, message: Message)
    async def on_file_upload(self, file: File)
    async def on_voice_message(self, voice: Voice)
    async def on_command(self, command: str)
    
    # Классификация и уточнения
    async def classify_content(self, content)
    async def ask_for_clarification(self, confidence, options)
    async def process_clarification_response(self, response)
    
    # Управление задачами
    async def get_today_tasks(self) -> List[Task]
    async def add_task(self, title, priority, object_id)
    async def mark_task_done(self, task_id)
    async def list_tasks(self, filter)
    
    # Ответы на вопросы
    async def answer_question(self, question: str)
    async def search_contacts(self, query)
    async def search_events(self, query)
    async def search_files(self, query)
    
    # Управление объектами
    async def set_active_object(self, object_name)
    async def add_participant(self, name, role)
    async def update_object_status(self, status)
    async def get_object_stats(self)

class ContentClassifier:
    async def classify_file(self, file_data) -> Classification
    async def classify_text(self, text) -> Classification
    async def classify_voice(self, audio) -> Classification
    async def refine_classification(self, user_feedback)

class FileManager:
    async def upload_to_storage(self, file, path)
    async def check_duplicate(self, file_hash)
    async def save_metadata(self, file_meta)
    async def version_file(self, file_id, new_version)
```

---

## 🌙 BATCH_ANALYZER_FULL.PY (500-700 строк)

**Запускается:** Каждый день в 2:00 AM UTC

### Главная Функция #1: Переанализ Всей БД

```python
async def daily_deep_analysis():
    """
    Переанализирует ВСЮ базу данных через Perplexity
    Обновляет классификацию, добавляет новые данные
    """
    
    # 1. Читает все данные
    contacts = await db.get_all('contacts')
    events = await db.get_all('events')
    files = await db.get_all('files_metadata')
    objects = await db.get_all('dynamic_objects')
    
    # 2. Для каждого контакта переанализирует всю историю
    for contact in contacts:
        history = await db.get_contact_history(contact.id)
        analysis = await perplexity.analyze_bulk({
            "name": contact.full_name,
            "history": history,
            "mentions": contact.mention_count
        })
        
        # Обновляет контакт новыми данными
        contact.organization = analysis.get("organization")
        contact.role = analysis.get("role")
        contact.context_tags = analysis.get("tags")
        contact.importance_score = analysis.get("importance")
        
        await db.update('contacts', contact)
    
    # 3. Для каждого события анализирует детали
    for event in events:
        analysis = await perplexity.analyze_event(event)
        event.importance = analysis.get("importance")
        event.summary = analysis.get("summary")
        await db.update('events', event)
    
    # 4. Для каждого файла переанализирует содержание
    for file in files:
        analysis = await perplexity.reanalyze_file(file)
        file.ai_classification = analysis
        await db.update('files_metadata', file)
```

---

### Главная Функция #2: Автоматическая Эволюция БД (Auto Schema)

```python
async def evolve_database_schema():
    """
    Анализирует какие новые поля нужны в таблицах
    Добавляет их автоматически без потери данных
    """
    
    # Пример 1: Нужно поле "budget" для проектов
    needed_fields = await analyze_what_fields_needed()
    
    for field in needed_fields:
        if not await db.column_exists(field.table, field.name):
            await db.add_column(
                table=field.table,
                name=field.name,
                type=field.type,
                default=field.default
            )
            logger.info(f"✅ Добавлено поле: {field.table}.{field.name}")
    
    # Пример 2: Нужна новая таблица "contracts"
    needed_tables = await analyze_what_tables_needed()
    
    for table in needed_tables:
        if not await db.table_exists(table.name):
            await db.create_table(table.name, table.schema)
            logger.info(f"✅ Создана таблица: {table.name}")
```

**Как работает:**
```
1. Читает всё, что добавляли пользователи
2. Спрашивает Perplexity: "Какие данные нужны?"
3. Автоматически добавляет новые поля/таблицы
4. БД развивается вместе с использованием!

Пример:
- Пользователь загружает контракты
- Batch видит это
- Создаёт таблицу "contracts"
- Добавляет поле "contract_type" в objects
- Всё готово!
```

---

### Главная Функция #3: Обучение на Истории

```python
async def learn_from_history():
    """
    Анализирует паттерны и генерирует инсайты
    """
    
    # Анализирует события за месяц
    events_month = await db.get_events(days=30)
    
    patterns = {
        "встречи_по_пятницам": count_events_on_day("Friday"),
        "проект_X_часто_обсуждается": count_mentions("MOS-01"),
        "бюджет_растет": analyze_trend("budget", direction="up"),
        "новые_люди_присоединяются": find_new_contacts(),
        "договоры_в_ноябре": count_by_month("contracts", "11"),
    }
    
    # Сохраняет как инсайты
    for pattern_name, pattern_data in patterns.items():
        await db.save_insight({
            "type": "pattern",
            "title": pattern_name,
            "description": pattern_data.description,
            "confidence": pattern_data.confidence,
            "evidence": pattern_data.events,
            "recommendation": pattern_data.recommendation
        })
    
    logger.info(f"✅ Найдено паттернов: {len(patterns)}")
```

---

### Главная Функция #4: Объединение Дублей

```python
async def merge_duplicates_smart():
    """
    Находит похожие контакты и объединяет с полной историей
    """
    
    # Находит потенциальные группы дублей
    duplicate_groups = await find_similar_contacts(
        similarity_threshold=0.85
    )
    
    for group in duplicate_groups:
        # "Ivan", "Ivan Petrov", "И.П." → одна запись
        
        # Объединяет с полной историей
        merged = await merge_contacts_smart(group, {
            "keep_all_history": True,
            "sum_mention_counts": True,
            "merge_tags": True,
            "keep_all_events": True
        })
        
        # Результат:
        merged_contact = {
            "id": merged.id,
            "full_name": "Ivan Petrov",
            "aliases": ["Ivan", "И.П.", "Ivan Petrov"],
            "mention_count": sum([c.mention_count for c in group]),
            "context_tags": merge_tags(group),
            "organization": most_common(group),
            "role": most_common(group),
            "events": all_events_from_group,
        }
        
        await db.save_contact(merged_contact)
        logger.info(f"✅ Объединено контактов: {len(group)} → 1")
```

---

### BATCH_ANALYZER_FULL.PY — Все Функции

```python
async def analyze_all_content()
    # Переанализ всех файлов, текстов, событий

async def evolve_database_schema()
    # Добавить нужные поля и таблицы автоматически

async def learn_from_history()
    # Найти паттерны и генерировать инсайты

async def merge_and_deduplicate()
    # Объединить дубли контактов

async def build_knowledge_graph()
    # Построить полный граф связей

async def generate_recommendations()
    # Что сделать дальше? Какие задачи нужны?

async def update_object_statistics()
    # Обновить статистику по проектам

async def cleanup_old_versions()
    # Очистить старые версии файлов

async def send_summary_to_user()
    # Отправить сводку в Telegram
```

---

## 🗄️ БАЗА ДАННЫХ v2 (Динамическая)

### Основные таблицы:

| Таблица | Назначение | Эволюция |
|---------|-----------|----------|
| **contacts** | Люди (Ivan, Maria, etc) | ✅ Добавляет поля |
| **dynamic_objects** | Проекты (MOS-01, etc) | ✅ Добавляет поля |
| **events** | События (встречи, звонки) | ✅ Новые типы событий |
| **tasks** | Задачи на день | ✅ Новые статусы |
| **files_metadata** | Загруженные файлы | ✅ Версионирование |
| **relationships** | Граф связей (кто с кем) | ✅ Новые типы связей |
| **user_memory** | Персональные данные | ✅ Новые ключи |
| **insights** | Инсайты и паттерны | AUTO (пополняется) |
| **analysis_history** | Логи анализа | AUTO |
| **schema_changes** | Изменения структуры БД | AUTO |

**Ключевое отличие:** Таблицы эволюционируют! Добавляются поля автоматически когда нужно!

---

## 🔧 ИНТЕГРАЦИИ

### 1. Perplexity API (Ум системы)

```python
# На каждое сообщение (реал-тайм)
await perplexity.classify(
    content=user_input,
    model="sonar-reasoning-pro"
) → category, confidence, tags

# На каждый файл
await perplexity.analyze_file(file)
→ classification, keywords, summary

# На ночь (батч)
await perplexity.analyze_bulk(
    contacts=all_contacts,
    batch_size=10
) → updated_fields, new_insights
```

### 2. Telegram Bot API (Интерфейс)

```python
# Получение
- Текст (сообщения)
- Голос (audio/ogg)
- Файлы (любые типы)
- Команды (/object, /today, etc)

# Отправка
- Текстовые сообщения
- Кнопки (выбор категории)
- Документы
- Списки задач
- Статистика
```

### 3. DigitalOcean Spaces (Хранилище файлов)

```python
# Upload
await storage.upload_file(
    file=user_file,
    path=f"files/{date}/{filename}",
    acl="private"
) → s3_url, file_hash

# Download
await storage.download_file(s3_url) → file_content

# List
await storage.list_files(prefix="files/2025/12")
→ [files...]
```

### 4. Supabase (БД)

```python
# CRUD
await db.insert('contacts', contact_data)
await db.get('contacts', id=123)
await db.update('contacts', id=123, data={...})
await db.query('SELECT * FROM events WHERE ...')

# Real-time
subscription = await db.subscribe('events', on_change=callback)

# Auth
token = await db.auth.login(email, password)
```

---

## 🔐 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ

```env
# TELEGRAM
TELEGRAM_BOT_TOKEN=1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh
DEFAULT_USER_ID=123456789

# SUPABASE
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# PERPLEXITY
PERPLEXITY_API_KEY=pplx-xxxxxxxxxxxxxxxxxxxxx
PERPLEXITY_MODEL=sonar-reasoning-pro

# DIGITALOCEAN SPACES (S3)
DIGITALOCEAN_SPACES_KEY=DO000XXXXXXXXXXXX
DIGITALOCEAN_SPACES_SECRET=xxxxxxxxxxxxxxxxxxxxx
DIGITALOCEAN_SPACES_ENDPOINT=nyc3.digitaloceanspaces.com
DIGITALOCEAN_SPACES_BUCKET=super-brain-files

# ENVIRONMENT
LOG_LEVEL=INFO
BATCH_RUN_HOUR=2
BATCH_RUN_MINUTE=0
AUTO_EVOLVE_SCHEMA=true
CLASSIFICATION_CONFIDENCE_THRESHOLD=70
MERGE_SIMILARITY_THRESHOLD=85
```

---

## 📈 ЖИЗНЕННЫЙ ЦИКЛ

### ДЕНЬ (Online — BOT.PY)

```
09:00
Пользователь: [загружает file.pdf]
        ↓
BOT: "Что это?"
        ↓
Пользователь: "Смета на MOS-01"
        ↓
BOT: ✅ Сохранено!

12:00
Пользователь: /today
        ↓
BOT: 📋 Задачи на день:
     1. Встреча с Иваном (14:00)
     2. Подготовить смету
     3. Ответить Марии
        ↓
Пользователь: /done 1
        ↓
BOT: ✅ Встреча отмечена!

16:00
Пользователь: "Сколько встреч с Иваном?"
        ↓
BOT: 👥 Встреч: 12
     Темы: Финансы (8), Планирование (4)
        ↓
Пользователь: "Какие файлы загружал?"
        ↓
BOT: 📄 15 файлов всего
     Последние: smeta_mos01.pdf, договор.docx

23:00 — БОТ СПИТ
```

### НОЧЬ (2:00 AM — BATCH_ANALYZER_FULL.PY)

```
02:00 — ЗАПУСК БАТЧА

02:05
✅ Переанализ всех контактов
   - Ivan обновлён: роль → "финансовый директор"
   - Maria обновлена: организация → "планирование"

02:15
✅ Проверка на дубли
   - Найдено: "Иван" + "Ivan Petrov"
   - Объединено → 1 запись
   - История сохранена полностью

02:25
✅ Анализ файлов
   - 50 файлов переанализировано
   - Улучшена классификация

02:35
✅ Эволюция БД
   - Добавлено поле: budget (в dynamic_objects)
   - Создана таблица: contracts

02:45
✅ Обучение на истории
   - Найдено 5 паттернов:
     • Встречи по пятницам
     • MOS-01 обсуждается часто
     • Новые люди присоединяются

03:00
✅ Инсайты созданы
✅ Граф связей обновлён
✅ Статистика обновлена

03:05 — ГОТОВО!
BOT отправляет сводку: "Ночной анализ завершен!"
```

---

## ✅ ПРИЕМОЧНЫЕ КРИТЕРИИ (Definition of Done)

### BOT.PY
- [ ] Получает текст, файлы, голос из Telegram
- [ ] Классификация файлов работает (>95% confidence)
- [ ] Спрашивает уточнения если нужно
- [ ] Файлы сохраняются с хешами для дедупликации
- [ ] /today выдаёт список задач с приоритетами
- [ ] Отвечает на вопросы из БД
- [ ] /object управляет проектами
- [ ] Все данные версионируются

### BATCH_ANALYZER_FULL.PY
- [ ] Запускается каждый день в 2:00 AM
- [ ] Переанализирует всю БД через Perplexity
- [ ] Добавляет новые поля/таблицы автоматически
- [ ] Объединяет дубли контактов
- [ ] Генерирует инсайты и паттерны
- [ ] Строит граф связей
- [ ] Отправляет сводку в Telegram

### Deployment
- [ ] Docker образ собирается и пушится в Registry
- [ ] K8s Deployment работает в production
- [ ] CronJob для батча работает ночью
- [ ] Логирование настроено (Prometheus)
- [ ] Health check отвечает
- [ ] GitHub Actions CI/CD работает на каждый push

---

## 🎬 TIMELINE

```
DAY 1 (2 часа)
├─ 0:00-0:30  bot.py основная структура
├─ 0:30-1:00  Классификация + уточнения
├─ 1:00-1:30  Управление задачами
└─ 1:30-2:00  Ответы на вопросы

DAY 2 (2 часа)
├─ 0:00-0:45  batch_analyzer_full.py основа
├─ 0:45-1:15  Эволюция БД + обучение
├─ 1:15-1:45  Объединение дублей
└─ 1:45-2:00  Тестирование

DAY 3 (2 часа)
├─ 0:00-0:30  Docker build
├─ 0:30-1:00  K8s deployment
└─ 1:00-2:00  GitHub Actions CI/CD

ИТОГО: 6 часов кодирования
```

---

## 🎯 ФИНАЛЬНЫЙ СТАТУС

**SUPER BRAIN v2.0 — ТЕХНИЧЕСКОЕ ЗАДАНИЕ УТВЕРЖДЕНО!** ✅

Это не просто Telegram бот — это ваш личный цифровой помощник!

Единая точка входа для всех ваших знаний, задач и размышлений.

---

**Дата утверждения:** 7 декабря 2025, 13:45 MSK  
**Версия:** 2.0  
**Статус:** APPROVED ✅  
**Автор:** Perplexity AI (vik9541)  
**Платформа:** DigitalOcean Kubernetes + Telegram