name: Secure Deploy to Kubernetes

# 🚫 ВРЕМЕННО ОТКЛЮЧЕН - будет активирован после добавления secrets
# See GITHUB_ACTIONS_SETUP.md для инструкций

on:
  # Все триггеры отключены
  workflow_dispatch:
    inputs:
      environment:
        description: 'NOT READY - Add secrets first'
        required: true
        default: 'disabled'

jobs:
  check-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Check Configuration
        run: |
          echo "❌ Deploy workflow is NOT READY"
          echo ""
          echo "To enable deployment:"
          echo "1. Open: Settings → Secrets and variables → Actions"
          echo "2. Add these 8 secrets:"
          echo "   - KUBECONFIG_PROD"
          echo "   - DO_API_TOKEN_PROD"
          echo "   - SUPABASE_URL"
          echo "   - SUPABASE_ANON_KEY"
          echo "   - SUPABASE_SERVICE_ROLE_KEY"
          echo "   - TELEGRAM_BOT_TOKEN"
          echo "   - DO_REGISTRY_USERNAME"
          echo "   - DO_REGISTRY_TOKEN"
          echo ""
          echo "3. See GITHUB_ACTIONS_SETUP.md for detailed instructions"
          echo ""
          exit 1
