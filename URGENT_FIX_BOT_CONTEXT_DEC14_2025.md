# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ: Бот игнорирует контекст

**Дата:** 14 декабря 2025, 09:01 MSK  
**Статус:** 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА  
**Приоритет:** HIGHEST  

---

## 🔴 ПРОБЛЕМА

Бот **НЕ РАБОТАЕТ** как персональный ассистент. Вместо этого:

❌ Отправляет **КАЖДОЕ** сообщение в Perplexity AI  
❌ Не распознаёт **интенты** (задача, контакт, чек, здоровье)  
❌ Не использует **контакты** из базы  
❌ Не ведёт **историю** по людям  
❌ Не сохраняет **контекст** разговора  
❌ Возвращает **длинные статьи** вместо коротких ответов  

### Примеры НЕПРАВИЛЬНОГО поведения:

```
USER: "Пришел на работу в 7.50"
BOT: [длинная статья про табель учёта рабочего времени, миграцию, ТК РФ...]

USER: "Я встречался вчера с Антоном Носковым"
BOT: [поиск в интернете про всех Антонов Носковых в РФ...]

USER: (отправляет фото)
BOT: "Поздравляю с покупкой! Опишите товар..."
```

---

## ✅ КАК ДОЛЖНО БЫТЬ

### Правильное поведение:

```
USER: "Пришел на работу в 7.50"
BOT: "📍 Записано: приход на работу в 7:50"

USER: "Я встречался вчера с Антоном Носковым, договорились сегодня созвониться"
BOT: "✅ Записано: встреча с Антоном Носковым (вчера), созвон сегодня"
     [кнопка: Антон Носков - подробнее]

USER: "Сравни с моими контактами, он там есть"
BOT: "✅ Антон Носков найден в ваших контактах:
     📱 +7 XXX XXX-XX-XX
     💼 Работает в Камчатке (Москва)
     
     Обновить информацию?"

USER: (отправляет фото чека)
BOT: "🧾 Чек: Пятёрочка, 385₽, 3 товара
     • Молоко 90₽
     • Хлеб 45₽
     • Филе 250₽"
```

---

## 🔧 ЧТО НУЖНО ИСПРАВИТЬ СРОЧНО

### PRIORITY 1: Отключить Perplexity AI из основного потока

**Файл:** `handlers/unified.py` или аналог

```python
# СЕЙЧАС (НЕПРАВИЛЬНО):
async def handle_message(update, context):
    text = update.message.text
    
    # ❌ УДАЛИТЬ ЭТО:
    response = await perplexity_search(text)  
    await update.message.reply_text(response)

# ДОЛЖНО БЫТЬ:
async def handle_message(update, context):
    text = update.message.text
    user_id = update.effective_user.id
    
    # 1. Определить ИНТЕНТ
    intent = await dispatcher.classify_intent(text)
    
    # 2. Маршрутизировать
    if intent == "WORK_LOG":
        response = await work_tracker.log_arrival(user_id, text)
    elif intent == "CONTACT_MENTION":
        response = await contacts.handle_mention(user_id, text)
    elif intent == "PHOTO_RECEIPT":
        response = await receipts.analyze(update.message.photo)
    elif intent == "SMALL_TALK":
        response = "👋 Привет! Что нужно сделать?"
    # ... другие интенты
    
    await update.message.reply_text(response)
```

---

### PRIORITY 2: Создать модуль распознавания интентов

**Новый файл:** `services/intent_classifier.py`

```python
class IntentClassifier:
    """Классификация намерений пользователя БЕЗ внешних API"""
    
    KEYWORDS = {
        'WORK_LOG': ['пришел на работу', 'приехал', 'прибыл', 'начал работу'],
        'CONTACT_MENTION': ['встречался с', 'созвонился с', 'говорил с', 'общался с'],
        'TASK': ['запиши задачу', 'сделать', 'нужно', 'не забыть'],
        'PROJECT': ['создай проект', 'новый проект', 'проект'],
        'HEALTH': ['съел', 'курил', 'пошел курить', 'тренировка', 'спал'],
        'RECEIPT': [],  # определяется по наличию фото
        'CONTACT_REQUEST': ['сравни с контактами', 'кто это', 'добавь контакт'],
        'SMALL_TALK': ['привет', 'здравствуй', 'как дела', 'спасибо']
    }
    
    def classify(self, text: str, has_photo: bool = False) -> str:
        """Определить интент БЕЗ обращения к внешним API"""
        text_lower = text.lower()
        
        # Фото всегда считаем чеком (если не сказано иначе)
        if has_photo:
            return 'RECEIPT'
        
        # Проверка по ключевым словам
        for intent, keywords in self.KEYWORDS.items():
            if any(kw in text_lower for kw in keywords):
                return intent
        
        # По умолчанию - неопределённое сообщение
        return 'UNKNOWN'
```

---

### PRIORITY 3: Модуль работы с контактами

**Новый файл:** `services/contacts_manager.py`

```python
class ContactsManager:
    """Управление контактами и контекстом"""
    
    def __init__(self, db):
        self.db = db
    
    async def find_contact(self, user_id: str, name: str) -> dict:
        """Найти контакт в базе пользователя"""
        # Поиск по имени/фамилии
        contacts = await self.db.search_contacts(
            user_id=user_id,
            query=name
        )
        
        if len(contacts) == 1:
            return contacts[0]
        elif len(contacts) > 1:
            return {'multiple': True, 'contacts': contacts}
        else:
            return None
    
    async def extract_person_name(self, text: str) -> str:
        """Извлечь имя человека из текста"""
        # Простой парсер
        patterns = [
            r'с (\w+ \w+)',  # "с Антоном Носковым"
            r'встречался (\w+ \w+)',
            r'созвонился (\w+ \w+)',
            r'говорил (\w+ \w+)'
        ]
        
        import re
        for pattern in patterns:
            match = re.search(pattern, text, re.IGNORECASE)
            if match:
                return match.group(1)
        return None
    
    async def add_interaction(self, user_id: str, contact_id: str, 
                             interaction_type: str, details: str):
        """Добавить взаимодействие с контактом"""
        await self.db.insert('contact_interactions', {
            'user_id': user_id,
            'contact_id': contact_id,
            'type': interaction_type,  # meeting, call, message
            'details': details,
            'timestamp': datetime.now()
        })
```

---

### PRIORITY 4: Обработчик контактных упоминаний

**Файл:** `handlers/contacts_handler.py`

```python
class ContactsHandler:
    
    async def handle_mention(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
        """Обработка упоминания контакта"""
        text = update.message.text
        user_id = update.effective_user.id
        
        # 1. Извлечь имя
        person_name = await self.contacts.extract_person_name(text)
        
        if not person_name:
            await update.message.reply_text("Не понял, о ком речь?")
            return
        
        # 2. Найти в контактах
        contact = await self.contacts.find_contact(user_id, person_name)
        
        if contact:
            # Контакт найден
            await self.contacts.add_interaction(
                user_id=user_id,
                contact_id=contact['id'],
                interaction_type='meeting',
                details=text
            )
            
            message = f"✅ Записано: {text}\n\n"
            message += f"👤 **{contact['full_name']}**\n"
            message += f"📱 {contact['phone']}\n"
            
            if contact.get('company'):
                message += f"💼 {contact['company']}\n"
            
            await update.message.reply_text(message, parse_mode='Markdown')
        
        else:
            # Контакт не найден - предложить добавить
            keyboard = [
                [InlineKeyboardButton("➕ Добавить контакт", 
                                     callback_data=f"add_contact:{person_name}")]
            ]
            reply_markup = InlineKeyboardMarkup(keyboard)
            
            await update.message.reply_text(
                f"❓ **{person_name}** не найден в контактах.\n"
                "Добавить?",
                parse_mode='Markdown',
                reply_markup=reply_markup
            )
```

---

### PRIORITY 5: Обработчик рабочего времени

**Файл:** `handlers/work_tracker.py`

```python
class WorkTracker:
    
    async def log_arrival(self, user_id: str, text: str) -> str:
        """Зафиксировать приход на работу"""
        
        # Извлечь время
        import re
        time_match = re.search(r'(\d{1,2})[:.](\\d{2})', text)
        
        if time_match:
            hour = time_match.group(1)
            minute = time_match.group(2)
            arrival_time = f"{hour}:{minute}"
        else:
            arrival_time = datetime.now().strftime("%H:%M")
        
        # Сохранить в БД
        await self.db.insert('work_logs', {
            'user_id': user_id,
            'type': 'arrival',
            'time': arrival_time,
            'date': date.today(),
            'raw_text': text
        })
        
        return f"📍 Записано: приход на работу в {arrival_time}"
```

---

### PRIORITY 6: Короткий /start

**Файл:** `handlers/commands.py`

```python
async def start(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    user_id = update.effective_user.id
    first_name = update.effective_user.first_name
    
    # Создать пользователя если не существует
    await self.db.ensure_user_exists(user_id)
    
    message = f"Привет, {first_name}! 👋\n\n"
    message += "Я твой личный ассистент.\n"
    message += "Просто напиши, что нужно:\n"
    message += '• "запиши задачу: купить молоко"\n'
    message += '• "создай проект Ремонт"\n'
    message += '• "пришел на работу в 8:00"\n'
    message += '• "встречался с Антоном"\n'
    message += '• отправь фото чека\n\n'
    message += "/help - если нужна справка"
    
    await update.message.reply_text(message)
```

---

## 📋 СРОЧНЫЕ ЗАДАЧИ ДЛЯ КОМАНДЫ

### ✅ TASK-URGENT-001: Убрать Perplexity из основного потока
- [ ] Найти где вызывается Perplexity/поисковый API
- [ ] Закомментировать/удалить эти вызовы
- [ ] Перплексити нужна ТОЛЬКО для команды `/search [запрос]`

### ✅ TASK-URGENT-002: Добавить IntentClassifier
- [ ] Создать `services/intent_classifier.py`
- [ ] Реализовать метод `classify(text, has_photo)`
- [ ] Протестировать на примерах из отчёта

### ✅ TASK-URGENT-003: Модуль контактов
- [ ] Создать `services/contacts_manager.py`
- [ ] Реализовать поиск по имени
- [ ] Реализовать извлечение имён из текста
- [ ] Добавить историю взаимодействий

### ✅ TASK-URGENT-004: Handlers для интентов
- [ ] `handlers/contacts_handler.py` - упоминания людей
- [ ] `handlers/work_tracker.py` - учёт рабочего времени
- [ ] Обновить `handlers/unified.py` - маршрутизация

### ✅ TASK-URGENT-005: Исправить /start
- [ ] Короткое приветствие (3-4 строки)
- [ ] Примеры команд
- [ ] Убрать длинные инструкции

---

## 🗄️ СТРУКТУРА БД (добавить таблицы)

```sql
-- Взаимодействия с контактами
CREATE TABLE contact_interactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR NOT NULL,
    contact_id UUID REFERENCES contacts(id),
    type VARCHAR, -- meeting, call, message
    details TEXT,
    timestamp TIMESTAMP DEFAULT NOW()
);

-- Рабочее время
CREATE TABLE work_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id VARCHAR NOT NULL,
    type VARCHAR, -- arrival, departure, break
    time TIME,
    date DATE,
    raw_text TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Контекст разговора (для последнего упомянутого человека)
CREATE TABLE conversation_context (
    user_id VARCHAR PRIMARY KEY,
    last_mentioned_contact_id UUID,
    last_mentioned_name VARCHAR,
    context_data JSONB,
    updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🧪 ТЕСТОВЫЕ СЦЕНАРИИ

После исправлений протестировать:

```
1. USER: "Пришел на работу в 7.50"
   ОЖИДАЕМО: "📍 Записано: приход на работу в 7:50"

2. USER: "Встречался вчера с Антоном Носковым"
   ОЖИДАЕМО: "✅ Записано: встреча с Антоном Носковым
              👤 Антон Носков
              📱 +7..."

3. USER: "Сравни с моими контактами"
   ОЖИДАЕМО: поиск в СВОЕЙ базе контактов, НЕ в интернете

4. USER: (отправил фото чека)
   ОЖИДАЕМО: OCR, парсинг, короткий ответ с суммой и товарами

5. USER: "Запиши задачу: купить молоко"
   ОЖИДАЕМО: "✅ Задача добавлена: купить молоко"

6. USER: "Привет"
   ОЖИДАЕМО: "👋 Привет! Что нужно сделать?"
```

---

## ⏰ ДЕДЛАЙН

**Сегодня, 14 декабря 2025, до 18:00 MSK**

Это критическая проблема - бот не работает как задумано.

---

## 📞 КОНТАКТЫ

Если возникли вопросы по реализации:
- Проверьте примеры кода выше
- Посмотрите на тестовые сценарии
- Приоритет: сначала убрать Perplexity, потом добавить интенты

---

**Статус:** 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА  
**Приоритет:** HIGHEST  
**Дедлайн:** 14 декабря 2025, 18:00 MSK  

> 🚨 **БОТ ДОЛЖЕН БЫТЬ ПЕРСОНАЛЬНЫМ АССИСТЕНТОМ, А НЕ ПОИСКОВИКОМ!**
