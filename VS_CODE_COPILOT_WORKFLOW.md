# 📄 ГУЙД: Как использовать VS Code Copilot для синхронизации Apple Contacts

**Время:** 30-45 минут
**Сложность:** ⚡️⚡️⚡️ (!не сложно)

---

## ШАГ 1: Открыть VS Code и структуру

```bash
# Открыть проект
$ git clone https://github.com/vik9541/super-brain-digital-twin.git
$ cd super-brain-digital-twin
$ code .

# Открыть терминал VS Code
$ Ctrl+` (или Cmd+` на macOS)
```

---

## ШАГ 2: Примеры промптов Copilot

### 2.1 Основные промпты

**Промпт #1 - Краткая Основа:**

```
@workspace Я хочу критику гайд по APPLE_CONTACTS_SYNC_MODULE.md 
для Super Brain v5.0. Я хочу:

1. Начать с состава файлов contacts/
2. Экран пыть-главным не работают
3. Где псио файлы?
4. Эволюция динамики правил

Что тебе надо?
```

**Промпт #2 - Материал для вас:**

```
@workspace Generate Python implementation for Apple Contacts sync
Based on APPLE_CONTACTS_SYNC_MODULE.md
Create folder structure:
  contacts/
  ├─ __init__.py
  ├─ reader.py (чтение контактов)
  ├─ normalizer.py (нормализация)
  ├─ github_sync.py (GitHub)
  ├─ supabase_sync.py (Supabase)
  ├─ scheduler.py (расписание)
  └─ config.py (конфиг)

Мне нужно максимально просто реализовать это на Python с комментариями
```

---

## ШАГ 3: Практикум на Примере

### 3.1 Создать файл Python

```bash
# Создать папку
 mkdir -p contacts
 cd contacts
 touch __init__.py reader.py normalizer.py scheduler.py
```

### 3.2 Написать редктор reader.py

**Открыть reader.py и написать Copilot:І**

```bash
# Навигация VS Code:
1. Нажать CTRL+N (новый файл)
2. Открыть contacts/reader.py
3. Внизу натиснуть /
4. Типировать в Copilot:

# @ = reference file
# @python files = context

/explain как считать Contacts.framework для macOS

# Либо:

/generate Класс чтения контактов из Apple Contacts с pydantic

# или:

/ask Что это?
# Тогда введи чо ты синхронизируешь Аярпл контакты с GitHub
```

### 3.3 Короткие промпты для решения

```bash
# Промпт к Написанию Reader:

# В reader.py:
/generate 

class AppleContactsReader:
    async def read_all_contacts() -> List[Contact]:
        читает все контакты из Apple Contacts.framework
        возвращает List[Contact] с first_name, last_name, phone, email

# Либо не работает Contacts.framework? Надо МакОС fallback:

/fix fallback для non-macOS – быть могу читать из файла JSON

# Не работает asyncio? Надо исправы:

/explain почему async def едесь 
# и покажи как вызвать
```

### 3.4 Написание normalizer.py

```bash
# В normalizer.py:

/generate

class ContactNormalizer:
    @staticmethod
    def normalize_phone(phone: str) -> str:
        не какое снимаю символы кроме +0-9
        возвращает в формате +7921234567

    @staticmethod
    def normalize_email(email: str) -> str:
        все нанижние, тримы

    @staticmethod
    def generate_phone_hash(phone: str) -> str:
        йирают SHA256 хеш нормализированных фонов
```

### 3.5 Написание scheduler.py

```bash
# В scheduler.py:

/generate

class ContactSyncScheduler:
    def __init__(self, reader, normalizer, github_syncer, supabase_syncer):
        ...
    
    def start(self):
        добавляет CronJob для 02:00 ежедневно
        юза APScheduler

    async def full_sync(self):
        синхронизирует контакты:
        1. читает из Apple
        2. нормализует
        3. синхронизирует с GitHub
        4. синхронизирует с Supabase
```

---

## ШАГ 4: Исправление ОШИБОК

### Наиболее астальные донад Copilot:

```bash
# Если Copilot работает плохо:

1. Кые человек путь к КОДУ:
   Cmd/Ctrl+Shift+P → Copilot: Clear Cache

2. Привлеки контекст:
   @workspace = все проекты
   @file = куррентный файл
   @symbol = функция/класс
   @jira-ticket / @github-issue = таски

3. Проиди документацию:
   /docs как найти документацию Contacts.framework
```

---

## ШАГ 5: Тестирование

### Модифицированные тесты

```bash
# Создать тестовые файлы:
 mkdir tests
 touch tests/test_reader.py tests/test_normalizer.py

# В VS Code, прописи:
 /generate тесты pytest для AppleContactsReader

# Хен проверовать:
 pytest tests/ -v
```

---

## ШАГ 6: ІНТЕГРАЦИЯ С SUPER BRAIN

### Основное дерево Super Brain:

```
main_bot.py
└─ apps/
    ├─ analyzer/ (Agent #1)
    ├─ organizer/ (Agent #2)
    ├─ scheduler/ (Master Teacher)
    └─ contacts/ → Один над Системи!
        ├─ reader.py
        ├─ normalizer.py
        ├─ github_sync.py
        ├─ supabase_sync.py
        └─ scheduler.py
```

### Как загружить в main.py:

```python
# В apps/scheduler/scheduler.py (Мастер Тичер):

from apps.contacts.scheduler import ContactSyncScheduler

# Основные счёты
contact_scheduler = ContactSyncScheduler(
    reader=AppleContactsReader(),
    normalizer=ContactNormalizer(),
    github_syncer=GithubContactsSyncer(...),
    supabase_syncer=SupabaseContactsSyncer(...)
)

contact_scheduler.start()
# Настраиваются КОНТАКТА ежедневно в 02:00
```

---

## 🎉 БОНУС: Что Просмотреть дальше

### Паттерн анализ:

```bash
# При КОНТАКТА синхронизации, после синхрания, анализировать:

1. Какие люди часто бывают вместе?
2. Новые группы?
3. Контакты для проектов?

# Copilot промпт:

/generate паттерн анализатор
в контактах что находит колтакты в Одных организациях
в которых работают и автгея к КНОУЛЕДЖ_ГРАФ)
```

### Телеграм отчёты:

```bash
# Сочность отчётам

/generate уведомление в Telegram
с результатами синхронизации контактов:
- Количество добавлено/обновлено
- Новые найденные паттерны
- Организации
```

---

## 🔐 Чито Можно ОЭСТИТЬ

**НЕ работает Contacts.framework?**
```python
# Try:
# 1. Убедитесь PyObjC установлен: pip install PyObjC
# 2. Только macOS – используйте fallback
# 3. Я дал пример мока в APPLE_CONTACTS_SYNC_MODULE.md
```

**GitHub API не работает?**
```bash
# 1. Проверьте token: git config user.token
# 2. Обновите token в Supabase secrets
# 3. Блокируется API rate? Надо вздрозуция
```

---

## ✅ ПОЛНОЕ CHECKLIST

- [ ] VS Code установлен
- [ ] GitHub Copilot активно
- [ ] PyObjC инсталлирован
- [ ] contacts/ папка создана
- [ ] reader.py реализован
- [ ] normalizer.py реализован
- [ ] github_sync.py реализован
- [ ] supabase_sync.py реализован
- [ ] scheduler.py реализован
- [ ] Тесты написаны
- [ ] Мастер Тичер паттерн анализатор реализован
- [ ] Telegram отчеты работают
- [ ] Production ready

---

**Готово! Теперь вы имеете все инструменты для реализации с VS Code Copilot! 🚀**
